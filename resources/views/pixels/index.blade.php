@extends('layouts.app')
@section('title', __('Retargeting pixels'))
@section('subtitle', __('Store approved marketing snippets, then opt-in on individual links.'))
@section('content')
<div class="detail-grid"><div class="card"><div class="card-header"><h2>{{ __('Add pixel') }}</h2></div><div class="card-body"><form method="POST" action="{{ route('pixels.store') }}" class="form-grid">@csrf <label class="field"><span>{{ __('Name') }}</span><input name="name" required placeholder="Meta admissions"></label><label class="field"><span>{{ __('Provider') }}</span><input name="provider" placeholder="Meta Pixel"></label><label class="field full"><span>{{ __('Snippet') }}</span><textarea name="snippet" rows="9" required placeholder="&lt;script&gt;...&lt;/script&gt;"></textarea><small>{{ __('Only add code you trust. Consent requirements remain your responsibility.') }}</small></label><div class="form-footer"><button class="button primary">{{ __('Save pixel') }}</button></div></form></div></div><div class="card"><div class="card-header"><h2>{{ __('Saved pixels') }}</h2></div><div class="card-body table-wrap"><table><thead><tr><th>{{ __('Name') }}</th><th>{{ __('Provider') }}</th><th></th></tr></thead><tbody>@forelse($pixels as $pixel)<tr><td>{{ $pixel->name }}</td><td>{{ $pixel->provider ?: '—' }}</td><td><form method="POST" action="{{ route('pixels.destroy', $pixel) }}">@csrf @method('DELETE')<button class="button danger small">{{ __('Delete') }}</button></form></td></tr>@empty<tr><td colspan="3">{{ __('No pixels yet.') }}</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
