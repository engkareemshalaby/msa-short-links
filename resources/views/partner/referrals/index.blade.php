@extends('layouts.partner')
@section('title', __('My students'))
@section('content')
<div class="partner-page-head"><div><h1>{{ __('My students') }}</h1><p>{{ __('Register a student or follow up on previously submitted details.') }}</p></div><a class="button primary" href="{{ route('partner.referrals.create') }}">＋ {{ __('Register a student') }}</a></div>
<div class="partner-card table-card"><div class="table-wrap"><table class="data-table"><thead><tr><th>{{ __('Student') }}</th><th>{{ __('Nationality') }}</th><th>{{ __('Program') }}</th><th>{{ __('Reference') }}</th><th>{{ __('Status') }}</th><th>{{ __('Submitted') }}</th><th></th></tr></thead><tbody>@forelse($referrals as $referral)<tr><td><strong>{{ $referral->student_name }}</strong><small class="link-destination" dir="ltr">{{ $referral->mobile }}</small></td><td>{{ $referral->nationality }}</td><td>{{ __($referral->desired_program) }}</td><td><code>{{ $referral->reference_code }}</code></td><td><span class="status-pill {{ $referral->status }}">{{ __(ucfirst($referral->status)) }}</span></td><td>{{ $referral->created_at->format('M d, Y') }}</td><td>@if(in_array($referral->status, ['new', 'incomplete'], true))<a class="button small" href="{{ route('partner.referrals.edit', $referral) }}">{{ __('Edit') }}</a>@endif</td></tr>@empty<tr><td colspan="7"><div class="empty-state"><strong>{{ __('No students registered yet') }}</strong>{{ __('Use the button above to register the first student.') }}</div></td></tr>@endforelse</tbody></table></div>@if($referrals->hasPages())<div class="pagination">{{ $referrals->links() }}</div>@endif</div>
@endsection
